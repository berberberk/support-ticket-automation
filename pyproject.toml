[project]
name = "support-ticket-automation"
version = "0.1.0"
description = "Safety-first PoC for support-ticket routing, grounded drafts, and human escalation."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "python-dotenv>=1.2.2",
    "qdrant-client[fastembed]>=1.19.0",
    "uvicorn>=0.52.1",
]

[tool.uv]
package = false
