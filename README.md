# Автоматизация обработки тикетов поддержки

## Проблема

Крупный онлайн-сервис получает около 200 тысяч тикетов поддержки в день через чат, email, веб-форму и мобильное приложение. Часть обращений типовая и может быть обработана быстрее, но рискованные, неоднозначные или содержащие PII, которую нельзя безопасно минимизировать, нельзя закрывать автоматически. Система должна ускорять классификацию и маршрутизацию, контролировать стоимость генерации ответов и сохранять аудит автоматических решений.

## Что делает предлагаемая система

Система принимает тикет, нормализует его и присваивает идентификатор запроса, выполняет проверки PII и жёстких рисков, а затем на горячем пути определяет тему, маршрут и уровень риска лёгкими детерминированными правилами и моделью классификации. Безопасные и достаточно уверенные случаи передаются в асинхронный контур: поиск по базе знаний или похожим решённым тикетам, подготовка черновика ответа и проверка его безопасности и обоснованности. Низкая уверенность, рискованная категория, отсутствие достаточных доказательств или отказ внешней зависимости переводят тикет оператору; каждое решение записывается в аудит.

## PoC и целевая архитектура

PoC подтверждает вертикальный сценарий на локальных и детерминированных компонентах: тестовое обращение, классификация и правила риска, небольшой локальный поиск по базе знаний, черновик или маршрут, аудит, happy path и эскалация рискованного либо low-confidence тикета.

Дополнительный демонстрационный режим может заменить локальный поиск на Qdrant с multilingual FastEmbed и шаблонный генератор на внешний OpenRouter BYOK-адаптер. По умолчанию этот режим выключен: обычный запуск не требует сети, ключа или векторной базы. Целевая архитектура дополнительно предусматривает масштабируемый горячий путь без состояния на экземпляре сервиса, надёжное хранилище событий и тикетов, очередь для медленного контура, боевые модели, контроль PII, мониторинг и операторский интерфейс.

## Скоуп на 4 часа

В PoC входят тестовое обращение, HTTP-интерфейс, идентификатор запроса, детерминированные правила риска, классификация темы и маршрута, локальный лексический поиск, детерминированный черновик на основе найденной статьи, журнал аудита, обычный сценарий и эскалация рискованных случаев оператору.

Только проектируются внешняя асинхронная LLM-интеграция, реальная реализация LLM outage/retry/backpressure, production broker и durable storage, production model serving, отдельный PII-сервис, полноценный monitoring stack, обучение моделей, операторский UI и MLOps-пайплайн. В PoC отказ локального генератора воспроизводится детерминированно; реальные outage/retry/backpressure остаются частью архитектурного дизайна.

`LOCAL_DECISION_BOUNDARY=0.60` и `MIN_RETRIEVAL_SCORE=0.20` — demo-only fixture boundaries; они не являются калиброванными production thresholds.

## Бизнес-ценность

Система снижает нагрузку на операторов за счёт ускорения типовых обращений и подготовки проверяемых черновиков. Быстрая маршрутизация помогает соблюдать SLA первого ответа даже при всплесках нагрузки. Ограничение автоматизации рискованными категориями и обязательный аудит защищают CSAT, SLA и безопасность пользователей. Асинхронный вызов LLM только там, где он действительно добавляет ценность, ограничивает стоимость и позволяет корректно переживать отказ внешнего сервиса.

Подробности целевой архитектуры и выбора ML/LLM-подходов находятся в [docs/architecture.md](docs/architecture.md) и [docs/ml.md](docs/ml.md).

## Быстрый запуск

Для HTTP-интерфейса требуется `uv` и Python 3.12; зависимости зафиксированы в `pyproject.toml` и `uv.lock`. Внешние сервисы и ключи не нужны.

Воспроизводимая проверка:

```bash
uv sync
uv run python demo.py
uv run python evaluate.py
uv run python -m unittest discover -s tests -v
```

HTTP-интерфейс:

```bash
uv run uvicorn app:app --host 127.0.0.1 --port 8000
```

Проверка состояния сервиса:

```bash
curl http://127.0.0.1:8000/health
```

Безопасное типовое обращение:

```bash
curl -X POST http://127.0.0.1:8000/tickets \
  -H 'Content-Type: application/json' \
  -d '{"request_id":"api-demo-1","channel":"web","text":"Не могу войти в аккаунт, как восстановить пароль?"}'
```

Рискованное обращение возвращает `needs_review`, маршрут `human_operator` и `draft: null`:

```bash
curl -X POST http://127.0.0.1:8000/tickets \
  -H 'Content-Type: application/json' \
  -d '{"request_id":"api-risk-1","channel":"web","text":"Мой аккаунт взломали, был несанкционированный вход."}'
```

В прототипе FastAPI-обработчик синхронно вызывает тот же локальный конвейер. Он показывает контракт сервиса, но не имитирует очередь или фоновых исполнителей. В целевой системе путь будет таким: API → быстрый путь → надёжная очередь → асинхронный медленный путь.

`python3 demo.py` и `python3 evaluate.py` также работают без `uv`: эти два сценария используют только стандартную библиотеку. Для HTTP-интерфейса используйте команды `uv` выше.

## Режимы PoC

Офлайн и воспроизводимо (значения по умолчанию):

```bash
uv sync
uv run python demo.py
uv run python evaluate.py
```

API:

```bash
uv run uvicorn app:app --host 127.0.0.1 --port 8000
```

Необязательный семантический поиск в локальном Qdrant:

```bash
cp .env.example .env
# в .env: RETRIEVAL_BACKEND=qdrant
uv run python demo_rag.py
```

Необязательная внешняя генерация по своему ключу: в `.env` задать `GENERATOR_BACKEND=openrouter`, `OPENROUTER_API_KEY` и при необходимости `OPENROUTER_MODEL`, затем повторить `uv run python demo_rag.py`. `.env` остаётся только локальным файлом и никогда не коммитится. Если ключа нет, фабрика сохраняет детерминированный генератор. Внешний адаптер получает только обезличенный текст тикета и одну найденную утверждённую статью; при пустом ответе, тайм-ауте или ошибке провайдера тикет уходит оператору без черновика.

## Артефакты

- [docs/architecture.md](docs/architecture.md) — архитектура и потоки
- [docs/ml.md](docs/ml.md) — выбор ML/LLM-подходов и оценка
- [docs/edge-cases.md](docs/edge-cases.md) — матрица решений и edge cases
- [docs/risks-and-ops.md](docs/risks-and-ops.md) — нагрузка, privacy и safety
- [docs/monitoring.md](docs/monitoring.md) — мониторинг и метрики пилота
- [app.py](app.py) — FastAPI-интерфейс `GET /health` и `POST /tickets`
- [demo_rag.py](demo_rag.py) — явная демонстрация выбранных retrieval/generator-адаптеров
- [.env.example](.env.example) — безопасный шаблон локальной конфигурации без секретов
- [pyproject.toml](pyproject.toml) и [uv.lock](uv.lock) — описание и зафиксированные зависимости
- [demo.py](demo.py) — три исполняемых сценария
- [evaluate.py](evaluate.py) — синтетическая демонстрационная проверка
- [tests/test_smoke.py](tests/test_smoke.py) — safety-инварианты
- [tests/test_api.py](tests/test_api.py) — контракт FastAPI-интерфейса

## Быстрая проверка

```bash
python3 demo.py
python3 evaluate.py
python3 -m unittest discover -s tests -v
```

`demo.py` показывает happy, risky/HITL и отказ генератора; `evaluate.py` запускает синтетическую демонстрационную проверку; `unittest` проверяет ключевые инварианты PoC.

### Что доказывает PoC

- детерминированную маршрутизацию и проверку риска на fast path;
- черновик на основе найденной статьи;
- HITL для hard-risk;
- безопасную деградацию при недоступности генератора;
- audit trail;
- синтетическую демонстрационную проверку.
- HTTP-контракт для обычного, рискованного и отказного сценариев.

## Ограничения PoC

- Риск определяется поиском подстрок и правилами.
- Санитизация PII показывает только email и телефон.
- Confidence — иллюстративный score, а не calibrated probability.
- Retrieval — пересечение токенов по четырём локальным статьям KB.
- Grounding gate проверяет, что детерминированный черновик содержит найденное evidence; это не семантическая оценка LLM.
- Отказ генератора — детерминированная локальная симуляция.
- Очередь, retries и идемпотентные внешние side effects относятся только к target design.
- Evaluation синтетический и малый; он не оценивает production quality.
- Внешний генератор не подключён: HTTP-интерфейс по умолчанию использует локальный детерминированный генератор.
- Локальный Qdrant/FastEmbed и OpenRouter — необязательные демонстрационные адаптеры, не production RAG. Семантическая оценка groundedness требует отдельного набора и ручной проверки.

План оценки и production-ограничения описаны в [docs/ml.md](docs/ml.md), [docs/monitoring.md](docs/monitoring.md) и [docs/edge-cases.md](docs/edge-cases.md).
